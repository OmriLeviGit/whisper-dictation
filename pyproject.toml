[project]
name = "whisper-recorder"
version = "0.1.0"
description = "Hold-to-record audio transcription"
requires-python = ">=3.11"

dependencies = [
    "sounddevice>=0.4.6",
    "soundfile>=0.12.1",
    "numpy>=1.24.0",
    "requests>=2.31.0",
    "pynput>=1.7.6",
]
